package descent.ui.text.outline;

import org.eclipse.jface.viewers.ViewerSorter;

public class DElementSorterByName extends ViewerSorter {
	
	/*
	@Override
	public int compare(Viewer viewer, Object e1, Object e2) {
		IElement elem1 = (IElement) e1;
		IElement elem2 = (IElement) e2;
		if (elem1.getStartLine() < elem2.getStartLine()) {
			return -1;
		} else if (elem1.getStartLine() > elem2.getStartLine()) {
			return 1;
		} else {
			if (elem1.getStartColumn() < elem2.getStartColumn()) {
				return -1;
			} else if (elem1.getStartColumn() > elem2.getStartColumn()) {
				return 1;
			} else {
				return 0;
			}
		}
	}
	*/

}
