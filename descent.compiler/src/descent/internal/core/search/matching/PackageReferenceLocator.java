package descent.internal.core.search.matching;

// TODO JDT search stub
public class PackageReferenceLocator extends PatternLocator {

	private final PackageReferencePattern pattern;

	public PackageReferenceLocator(PackageReferencePattern pattern) {
		super(pattern);
		
		this.pattern = pattern;
	}

}
